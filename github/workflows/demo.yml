name: Test workflow
on:
  push: 
    branches: ["**"]
  pull_request:
    branches: ["**"]
  workflow_dispatch: 
jobs: 
  build: ubuntu-latest,
  steps: actions/checkout@v4,
  name: Run a one-line script
  run: echo "Hello world"

